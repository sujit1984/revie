name: Deploy Staging

on:
  push:
    branches: [develop]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Placeholder staging deploy
        run: |
          echo "Hook this workflow to Vercel/Render/Fly staging deployments"
          echo "Run migrations before API rollout"
